# testbed/config/ — configuration templates
#
# Placeholders for now. Will be populated with:
#   - hostapd.conf
#   - wpa_supplicant.conf
#   - cloud-agent env files (verifier.env, issuer.env, holder.env)
#   - Cardano node config (config.json, topology.json, genesis files)
#
# TODO: migrate from hostap testbed-redesign branch
