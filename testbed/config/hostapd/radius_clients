# RADIUS clients allowed to query this server.
# Format:  <network/mask>   <shared-secret>
# Authenticator and RADIUS share the same container, so the only client is
# the local authenticator over loopback.
127.0.0.1/32   testing123
